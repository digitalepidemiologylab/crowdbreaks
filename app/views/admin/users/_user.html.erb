<tr>
  <td><%= user.username %></td>
  <td><%= user.email %></td>
  <td><%= role_badge(user.role) %></td>
  <td>
    <span> 
      <%= link_to fa_icon("pencil"), edit_admin_user_path(user.id) %>
    </span>
  </td>
</tr>
